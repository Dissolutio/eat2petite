import styled from 'styled-components'

const GalleryStyleContainer = styled.div`
    color: var(--font-light, #fff);
    a {
        color: var(--font-light, #fff);
    }
    a:visited {
        color: var(--gray2);
    }
`
export default GalleryStyleContainer
